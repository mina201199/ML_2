# 相容版本下限，非鎖版；目前驗證環境見 reports/metrics/environment.json
#
# 第一行的 "." 是給託管平台用的：Streamlit Community Cloud 只跑
# pip install -r requirements.txt，不會跑 pip install -e .，
# 少了它 app/streamlit_app.py 的 `import secom` 會失敗。
# 本機開發請用 pip install -e ".[dev]"。
.
pandas>=2.2
numpy>=2.0
pyarrow>=17
scikit-learn>=1.5
lightgbm>=4.5
shap>=0.46
matplotlib>=3.9
streamlit>=1.50
altair>=5.4
duckdb>=1.0
scipy>=1.11
PyYAML>=6.0
joblib>=1.4
pytest>=8.0
